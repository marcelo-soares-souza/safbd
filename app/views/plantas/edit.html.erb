<ul class="nav nav-tabs">
  <li class="nav-item">
    <%= link_to t(:all), plantas_path, :class => "nav-link" %>
  </li>
  <% if user_signed_in? %>
    <li class="nav-item">
      <%= link_to t(:new), new_planta_path, :class => "nav-link" %>
    </li>
  <% end %>
  <li class="nav-item">
    <%= link_to "Editando #{@planta.nome}", planta_path(@planta), :class => "nav-link active" %>
  </li>
</ul>

<br/>

<%= render 'form', planta: @planta %>
