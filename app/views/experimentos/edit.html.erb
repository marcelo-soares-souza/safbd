<ul class="nav nav-tabs">
  <li class="nav-item">
    <%= link_to t(:all), experimentos_path, :class => "nav-link" %>
  </li>
  <% if user_signed_in? %>
    <li class="nav-item">
      <%= link_to t(:new), new_experimento_path, :class => "nav-link" %>
    </li>
  <% end %>
  <li class="nav-item">
    <%= link_to "Editando #{@experimento.nome}", experimento_path(@experimento), :class => "nav-link active" %>
  </li>
</ul>

<br/>

<%= render 'form', experimento: @experimento %>
